import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "01-01-first-circle",
  title: "1. 最初の円を描く",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.FIRST_CIRCLE,
      label: "実行結果をみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
  fill(255, 0, 0);
  ellipse(200, 200, 200, 200);
}`
    }
  ]
};

まずは、いちばん簡単なプログラムを書いてみましょう。

次のページでは、以下のようなプログラムを実行します

```javascript
function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
  fill(255, 0, 0);
  ellipse(200, 200, 200, 200);
}
```

<Exercise id={EXERCISE_IDS.FIRST_CIRCLE} />

実行ボタンをクリックして、実行結果をみてみましょう。
