import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "08-03-challenge-loops",
  title: "8-3. 👊チャレンジ⑧ 5つの円を並べよう",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.CHALLENGE_LOOPS,
      label: "コードを書いてみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
  fill(255, 0, 0);

  // for を使って5つの円を横に並べよう

}`
    }
  ]
};

## 👊チャレンジ⑧

**for を使って5つの円を横一列に並べましょう**

### 条件

- 円を5つ描きます
- 横一列に等間隔で並べます
- 画面に収まるようにします

### ヒント

```javascript
for (let i = 0; i < 5; i = i + 1) {
  ellipse(50 + i * 70, 200, 50, 50);
}
```

- `i < 5` で5回繰り返します（i = 0, 1, 2, 3, 4）
- `50 + i * 70` で x 座標を計算します
  - 開始位置 50 から、70 ピクセルずつ離して配置
- 円のサイズは 50x50

### 応用

できたら、以下も試してみましょう：
- 円の数を変える（10個にしてみる）
- 間隔を変える（もっと密集させる / 離す）
- 縦に並べる（y 座標を変える）
