export const frontmatter = {
  id: "08-03-challenge-loops",
  title: "8-3. 👊チャレンジ⑧ 5つの円を並べよう",
  category: "basic",
  initialCode: `function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
  fill(255, 0, 0);

  // for を使って5つの円を横に並べよう

}`,
  solution: `function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);
  fill(255, 0, 0);

  for (let i = 0; i < 5; i = i + 1) {
    ellipse(50 + i * 70, 200, 50, 50);
  }
}`,
  hints: [
    "i < 5 で5回繰り返す",
    "50 + i * 70 で等間隔に並ぶ（70ピクセルずつ離す）",
    "円のサイズは 50x50 ぐらいがちょうどいいよ"
  ]
};

## 👊チャレンジ⑧

**for を使って5つの円を横一列に並べよう**

### 条件

- 円を5つ描く
- 横一列に等間隔で並べる
- 画面に収まるようにする

### ヒント

```javascript
for (let i = 0; i < 5; i = i + 1) {
  ellipse(50 + i * 70, 200, 50, 50);
}
```

- `i < 5` で5回繰り返す（i = 0, 1, 2, 3, 4）
- `50 + i * 70` で x 座標を計算
  - 開始位置 50 から、70 ピクセルずつ離して配置
- 円のサイズは 50x50

### 応用

できたら、以下も試してみよう：
- 円の数を変える（10個にしてみる）
- 間隔を変える（もっと密集させる / 離す）
- 縦に並べる（y 座標を変える）
