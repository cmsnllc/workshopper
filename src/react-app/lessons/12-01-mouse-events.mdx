import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "12-01-mouse-events",
  title: "1. 指定した位置に図形を動かす",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.MOUSE_EVENTS,
      label: "試してみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
}

function mouseClicked() {
  background(255, 255, 255);
  fill(255, 0, 0);
  ellipse(mouseX, mouseY, 50, 50);
}`
    }
  ]
};

## 指定した位置に図形を動かす

ここまではプログラムを実行したら眺めているだけでしたが、何らかのアクションを元に描画することができます。

<Exercise id={EXERCISE_IDS.MOUSE_EVENTS} />
