export const frontmatter = {
  id: "01-03-challenge-yellow",
  title: "3. 👊チャレンジ① 黄色の円を描こう",
  category: "basic",
  initialCode: `function setup() {
  createCanvas(400, 400);
}

function draw() {
background(255, 255, 255);
fill(255, 0, 0);
ellipse(200, 200, 200, 200);
}`
};

## 👊 チャレンジ ①

**黄色を表示させたい時は、3 つの値をどう設定するべきでしょう？**

正解だと思う数値に、プログラムを書き換えてみましょう。
