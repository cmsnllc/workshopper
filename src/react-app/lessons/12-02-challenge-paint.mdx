import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "12-02-challenge-paint",
  title: "2. プログラムの解説",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.CHALLENGE_PAINT,
      label: "試してみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
}

function mouseClicked() {
  background(255, 255, 255);
  fill(255, 0, 0);
  ellipse(mouseX, mouseY, 50, 50);
}`
    }
  ]
};

## プログラムの解説
mouseClicked は、マウスがクリックされたら｛｝（波かっこ）の内容を実行するものです。また、mouseX と mouseY は特殊な変数で、現在のカーソルの位置を x,y 座標で取得できます。よって、マウスがクリックされたらマウスの位置に丸を描画するプログラムになります。

## 🤔試してみよう④

`background(255, 255, 255);` の行を消すとどうなるだろう？

<Exercise id={EXERCISE_IDS.CHALLENGE_PAINT} />
