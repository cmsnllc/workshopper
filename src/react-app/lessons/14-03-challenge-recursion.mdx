export const frontmatter = {
  id: "14-03-challenge-recursion",
  title: "14-3. 👊チャレンジ⑭ 十字パターン",
  category: "challenge",
  initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);
  circles(200, 200, 60, 3);
}

function circles(x, y, r, n) {
  ellipse(x, y, r * 2, r * 2);

  if (n < 1) {
    return;
  }

  let dr = r / 2;

  // 右側に小さい円
  circles(x + dr, y, dr, n - 1);

  // 左側に小さい円
  circles(x - dr, y, dr, n - 1);

  // 上下にも円を描いてみよう！

}`
};

## 👊チャレンジ⑭

**上下にも円を描いて、十字のパターンを作ろう**

### 条件

- 右・左だけでなく、上・下にも円を描く
- 十字のフラクタル図形になる

### ヒント

```javascript
circles(x + dr, y, dr, n - 1);  // 右
circles(x - dr, y, dr, n - 1);  // 左
circles(x, y + dr, dr, n - 1);  // 下
circles(x, y - dr, dr, n - 1);  // 上
```

- 右: x を増やす（`x + dr`）
- 左: x を減らす（`x - dr`）
- 下: y を増やす（`y + dr`）
- 上: y を減らす（`y - dr`）

### ポイント

- 4方向に円を描くと、十字のパターンになる
- 再帰呼び出しが4つになる
  - 1つの円から4つの円が生まれる
  - その4つの円からさらに4つずつ...

### 応用

できたら、以下も試してみよう：
- n の値を変える（2, 3, 4...）
- r の値を変える
- 色を変える（fill() を追加）
- 斜めにも円を描く（8方向）
