import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "14-01-recursion-intro",
  title: "1. 再帰関数とは",
  category: "advanced",
  exercises: [
    {
      id: EXERCISE_IDS.RECURSION_INTRO,
      label: "試してみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);

  circles(200, 200, 100, 3);
}

function circles(x, y, r, n) {
  ellipse(x, y, r * 2, r * 2);

  if (n < 1) {
    return;
  }

  let dr = r / 2;
  circles(x + dr, y, dr, n - 1);
  circles(x - dr, y, dr, n - 1);
}`
    }
  ]
};

## 再帰関数とは

定義した関数の中で、関数をふたたび呼び出すような関数のことを再帰関数といいます。

<Exercise id={EXERCISE_IDS.RECURSION_INTRO} />

