import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "06-03-loops-practice",
  title: "3. ループで円を並べよう",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.LOOPS_PRACTICE,
      label: "コードを書いてみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);
  fill(255, 0, 0);

  // for を使って3つの円を横に並べよう
}`
    } , {
      id: EXERCISE_IDS.CHALLENGE_LOOPS,
      label: "チャレンジ →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);
  fill(255, 0, 0);

  // ここにコードを書いてみよう
}`
    }
  ]
};

## ループで円を並べよう

for ループを使って、複数の円を等間隔で並べてみましょう。

### 目標

3つの赤い円を横一列に並べます。

### ヒント

```javascript
for (let i = 0; i < 3; i = i + 1) {
  ellipse(100 + i * 100, 200, 80, 80);
}
```

- `i < 3` で3回繰り返します（i = 0, 1, 2）
- `100 + i * 100` で x 座標を計算します
  - i = 0 のとき: 100 + 0 * 100 = 100
  - i = 1 のとき: 100 + 1 * 100 = 200
  - i = 2 のとき: 100 + 2 * 100 = 300

<Exercise id={EXERCISE_IDS.LOOPS_PRACTICE} />

## 👊チャレンジ⑤

以下のようなドット模様を描画してみよう。

<img src="/images/image3.jpg" alt="dots" style={{ maxWidth: '400px', width: '100%', display: 'block', margin: '16px 0' }} />

<Exercise id={EXERCISE_IDS.CHALLENGE_LOOPS} />
