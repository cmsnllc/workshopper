import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "06-03-loops-practice",
  title: "3. ループで円を並べよう",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.LOOPS_PRACTICE,
      label: "コードを書いてみる →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);
  fill(255, 0, 0);

  // for を使って3つの円を横に並べよう
}`
    } , {
      id: EXERCISE_IDS.CHALLENGE_LOOPS,
      label: "チャレンジ →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  background(255, 255, 255);
  fill(255, 0, 0);

  // ここにコードを書いてみよう
}`
    }
  ]
};

## 👊チャレンジ⑤

以下のようなドット模様を描画してみよう。

<img src="/images/image3.jpg" alt="dots" style={{ maxWidth: '400px', width: '100%', display: 'block', margin: '16px 0' }} />

<Exercise id={EXERCISE_IDS.CHALLENGE_LOOPS} />
