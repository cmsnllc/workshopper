import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "15-01-free-project",
  title: "自由課題",
  category: "advanced",
  exercises: [
    {
      id: EXERCISE_IDS.FREE_PROJECT,
      label: "自由制作 →",
      initialCode: `function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255, 255, 255);

  // ここに自由にコードを書いてみよう！

}`
    }
  ]
};

## 自由制作

ここまでできたら、あとは自由にプログラムを書いてみましょう。

<Exercise id={EXERCISE_IDS.FREE_PROJECT} />

アイデアが浮かばない人のために、以下に参考になるような図形を示します。

<img src="/images/image9.jpg" style={{ maxWidth: "400px", border: "1px solid #ccc" }} />

**シェルビンスキのギャスケット(n=3)**

必要になる関数

`triangle`

`x1`, `y1`, `x2`, `y2`, `x3`, `y3`

3点を指定して三角形を描画

`sin`, `cos`, `tan`

それぞれ三角関数

`PI`

円周率

<img src="/images/image5.jpg" style={{ maxWidth: "400px", border: "1px solid #ccc" }} />

**コッホ曲線(n=4)**

必要になる関数

`line`

`x1`, `y1`, `x2`, `y2`

2点を指定して線を描画
