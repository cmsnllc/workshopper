import { EXERCISE_IDS } from "../constants/exercises";

export const frontmatter = {
  id: "11-01-random",
  title: "ランダムに動かそう",
  category: "basic",
  exercises: [
    {
      id: EXERCISE_IDS.RANDOM,
      label: "確認する →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  frameRate(10);
}

function draw() {
  background(255, 255, 255);

  let x = random(400);
  let y = random(400);

  fill(255, 0, 0);
  ellipse(x, y, 50, 50);
}`
    }, {
      id: EXERCISE_IDS.RANDOM_PRACTICE,
      label: "色も変える →",
      initialCode: `function setup() {
  createCanvas(400, 400);
  frameRate(10);
}

function draw() {
  background(255, 255, 255);

  let x = random(400);
  let y = random(400);

  fill(255, 0, 0);
  ellipse(x, y, 50, 50);
}`
    }
  ]
};

## 乱数とは

これまで座標等の数値は固定の値として与えていましたが、ランダムな値を使ってみましょう。

### random() 関数

```javascript
random(400);  // 0 から 400 までのランダムな値
random(255);  // 0 から 255 までのランダムな値
```

`random()` は、毎回呼ばれるたびに**異なる値**を返します。

### ランダムな位置に描く

```javascript
let x = random(400);
let y = random(400);
ellipse(x, y, 50, 50);
```

円がランダムな位置に描かれます。draw が繰り返し実行されるので、毎回違う場所に現れます。

<Exercise id={EXERCISE_IDS.RANDOM} />

## 👊チャレンジ⑥

丸の色もランダムにしてみよう

<Exercise id={EXERCISE_IDS.RANDOM_PRACTICE} />
